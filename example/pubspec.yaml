name: example
description: An example project for the lazy loader

dependencies:
  flutter:
    sdk: flutter

  lazy_load_scrollview:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
