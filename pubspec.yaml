name: lazy_load_scrollview
description: A wrapper for a ScrollView that will trigger a callback when the bottom of the page is reached
version: 1.2.0
homepage: https://github.com/QuirijnGB/lazy-load-scrollview

environment:
  sdk: '>=2.0.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
